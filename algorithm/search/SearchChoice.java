package algorithm.search;

public class SearchChoice {
    public ArrSearch arrSearch(int arr[], int key) {
        return arrSearch(arr, key);
    }

    public BinarySearch binarySearch(int arr[], int key) {
        return binarySearch(arr, key);
    }
    SearchChoice(BinarySearch binarySearch){

    }
    SearchChoice(ArrSearch arrSearch){

    }
}
